export class Requester {

    address: string;
    name: string;
    url: string;

    constructor(address: string, name: string, url: string) {
        this.address = address;
        this.name = name;
        this.url = url;
    }
}
